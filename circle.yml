dependencies:
      override:
            - virtualenv pelican
            - source pelican/bin/activate
            - pip install pelican webassets cssmin
test:
      override:
            - make html
